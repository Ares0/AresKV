


    
     db不同于ZK，单个从也可做主，不能用从选举；
    加入Sentinel监视，由其负责心跳、选举。
     
     sentinel建立对DB的连接，进行心跳；
    多个sentinel，一次写，半数以上为leader，由leader负责监视。
    
     KV服务器端口8075，主服务器端口8076，
    从服务器选举端口8077，从服务器同步依次递增。
    
       客户端保存cid，职责分配有点牵强；
    没有kvObject验证，安全性不好，加入ValidateHandler；
  dump直接访问Map，存在写入频繁，可能造成dump一直完不成问题。
  
     分片集群，无中心、动态指定、重新分片、高可用复制；
  主从模式，主从复制、哨兵监视、哨兵集群、投票下线。